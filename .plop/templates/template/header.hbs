// components/molecules/{{pascalCase name}}/index.tsx

import React from "react";
import {{pascalCase name}}Props from "./index.types";
import Link from 'next/link';
import Image from "next/image";
import {templateConfig} from "@{{kebabCase name}}/config";
import { Button } from '@global-ui/button';


const Header: React.FC<{{pascalCase name}}Props> = () => {
  return (
      <header className="border-b">
        <div className="container mx-auto flex h-16 items-center justify-between">
          {/* Logo on the left */}
          <div className="flex items-center">
            <Link href="/" className="flex items-center space-x-2">
              <Image src="/logo_base.png" alt="Logo" width={40} height={40} className="rounded" />
            </Link>
          </div>

          {/* Navigation in the center */}
          <nav className="mx-6 flex items-center space-x-4 lg:space-x-6">
           {templateConfig.menuItems.map((item) => (
            <Link
              key={item.label}
              href={item.link}
            >
            {item.label}
            </Link>
            ))}
          </nav>

          {/* Button on the right */}
          <div className="flex items-center">
            <Button>Get Started</Button>
          </div>
        </div>
    </header>
  );
};

export default Header;